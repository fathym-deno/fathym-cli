{
  "tasks": {
    "run": "ftm run",
    "build": "deno task build:fmt && deno task build:lint && deno task build:cli && deno task test",
    "compile": "ftm compile",
    "install": "ftm install --useHome --to=.bin",
    "release": "deno task build && deno task compile && deno task install",
    "test": "ftm test",
    "deploy": "npx rimraf cov && deno task build && deno task publish:check && fathym git",
    "build:cli": "ftm build",
    "build:fmt": "deno fmt",
    "build:lint": "deno lint",
    "publish:check": "deno publish --dry-run --allow-dirty"
  },
  "imports": {
    "@fathym/common": "jsr:@fathym/common@0.2.299",
    "@fathym/cli": "jsr:@fathym/cli@0.0.81",
    "@fathym/dfs": "jsr:@fathym/dfs@0.0.48",
    "@std/path": "jsr:@std/path@1.1.3",
    "zod": "npm:zod@4.1.13"
  },
  "lock": false
}
