import { CommandIntents } from '@fathym/cli';
import ManageCommand from '../commands/manage.ts';

const cmd = ManageCommand.Build();
const origin = import.meta.resolve('../.cli.ts');

CommandIntents('Manage Command Suite', cmd, origin)
  .Intent('Show management status', (int) =>
    int
      .Args([])
      .Flags({})
      .ExpectLogs('📊 Showing management status...')
      .ExpectExit(0),
  )
  .Intent('Show verbose management status', (int) =>
    int
      .Args([])
      .Flags({ verbose: true })
      .ExpectLogs('📊 Showing management status...', 'Detailed view enabled')
      .ExpectExit(0),
  )
  .Intent('Dry run management status', (int) =>
    int
      .Args([])
      .Flags({ 'dry-run': true })
      .ExpectLogs('🛑 Dry run: Would show management status')
      .ExpectExit(0),
  )
  .Run();
